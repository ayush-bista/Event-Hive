<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/src/main/java/controller/LoginServlet.java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/src/main/java/controller/LoginServlet.java" />
              <option name="originalContent" value="package controller;&#10;&#10;import dao.UserDAO;&#10;import jakarta.servlet.ServletException;&#10;import jakarta.servlet.annotation.WebServlet;&#10;import jakarta.servlet.http.*;&#10;import model.User;&#10;import util.PasswordUtil;&#10;import util.ValidationUtil;&#10;&#10;import java.io.IOException;&#10;&#10;/**&#10; * LoginServlet - Handles GET (show form) and POST (process login).&#10; * URL: /login&#10; */&#10;@WebServlet(&quot;/login&quot;)&#10;public class LoginServlet extends HttpServlet {&#10;&#10;    private final UserDAO userDAO = new UserDAO();&#10;&#10;    @Override&#10;    protected void doGet(HttpServletRequest req, HttpServletResponse res)&#10;            throws ServletException, IOException {&#10;        // If already logged in, redirect to appropriate dashboard&#10;        HttpSession session = req.getSession(false);&#10;        if (session != null &amp;&amp; session.getAttribute(&quot;loggedInUser&quot;) != null) {&#10;            User user = (User) session.getAttribute(&quot;loggedInUser&quot;);&#10;            redirect(user, req, res);&#10;            return;&#10;        }&#10;        req.getRequestDispatcher(&quot;/WEB-INF/views/login.jsp&quot;).forward(req, res);&#10;    }&#10;&#10;    @Override&#10;    protected void doPost(HttpServletRequest req, HttpServletResponse res)&#10;            throws ServletException, IOException {&#10;&#10;        String email    = ValidationUtil.sanitize(req.getParameter(&quot;email&quot;));&#10;        String password = req.getParameter(&quot;password&quot;);&#10;&#10;        // Basic input check&#10;        if (!ValidationUtil.isValidEmail(email) || !ValidationUtil.isNotEmpty(password)) {&#10;            req.setAttribute(&quot;error&quot;, &quot;Please enter a valid email and password.&quot;);&#10;            req.getRequestDispatcher(&quot;/WEB-INF/views/login.jsp&quot;).forward(req, res);&#10;            return;&#10;        }&#10;&#10;        try {&#10;            User user = userDAO.findByEmail(email);&#10;&#10;            if (user == null || !PasswordUtil.verify(password, user.getPassword())) {&#10;                req.setAttribute(&quot;error&quot;, &quot;Invalid email or password.&quot;);&#10;                req.getRequestDispatcher(&quot;/WEB-INF/views/login.jsp&quot;).forward(req, res);&#10;                return;&#10;            }&#10;&#10;            if (user.isStudent() &amp;&amp; !user.isApprovedUser()) {&#10;                req.setAttribute(&quot;error&quot;, &quot;Your account is pending admin approval.&quot;);&#10;                req.getRequestDispatcher(&quot;/WEB-INF/views/login.jsp&quot;).forward(req, res);&#10;                return;&#10;            }&#10;&#10;            // Create session&#10;            HttpSession session = req.getSession(true);&#10;            session.setAttribute(&quot;loggedInUser&quot;, user);&#10;            session.setMaxInactiveInterval(30 * 60);  // 30 minutes&#10;&#10;            // Set remember-me cookie (optional)&#10;            String rememberMe = req.getParameter(&quot;rememberMe&quot;);&#10;            if (&quot;on&quot;.equals(rememberMe)) {&#10;                Cookie cookie = new Cookie(&quot;eh_email&quot;, email);&#10;                cookie.setMaxAge(7 * 24 * 60 * 60);  // 7 days&#10;                cookie.setPath(req.getContextPath());&#10;                cookie.setHttpOnly(true);&#10;                res.addCookie(cookie);&#10;            }&#10;&#10;            redirect(user, req, res);&#10;&#10;        } catch (Exception e) {&#10;            req.setAttribute(&quot;error&quot;, &quot;A server error occurred. Please try again.&quot;);&#10;            req.getRequestDispatcher(&quot;/WEB-INF/views/login.jsp&quot;).forward(req, res);&#10;        }&#10;    }&#10;&#10;    private void redirect(User user, HttpServletRequest req, HttpServletResponse res)&#10;            throws IOException {&#10;        String ctx = req.getContextPath();&#10;        if (user.isAdmin())   res.sendRedirect(ctx + &quot;/admin/dashboard&quot;);&#10;        else                  res.sendRedirect(ctx + &quot;/student/dashboard&quot;);&#10;    }&#10;}&#10;" />
              <option name="updatedContent" value="package controller;&#13;&#10;&#13;&#10;import dao.UserDAO;&#13;&#10;import jakarta.servlet.ServletException;&#13;&#10;import jakarta.servlet.annotation.WebServlet;&#13;&#10;import jakarta.servlet.http.*;&#13;&#10;import model.User;&#13;&#10;import util.PasswordUtil;&#13;&#10;import util.ValidationUtil;&#13;&#10;&#13;&#10;import java.io.IOException;&#13;&#10;&#13;&#10;/**&#13;&#10; * LoginServlet - Handles GET (show form) and POST (process login).&#13;&#10; * URL: /login&#13;&#10; */&#13;&#10;@WebServlet(&quot;/login&quot;)&#13;&#10;public class LoginServlet extends HttpServlet {&#13;&#10;&#13;&#10;    private final UserDAO userDAO = new UserDAO();&#13;&#10;&#13;&#10;    @Override&#13;&#10;    protected void doGet(HttpServletRequest req, HttpServletResponse res)&#13;&#10;            throws ServletException, IOException {&#13;&#10;        // If already logged in, redirect to appropriate dashboard&#13;&#10;        HttpSession session = req.getSession(false);&#13;&#10;        if (session != null &amp;&amp; session.getAttribute(&quot;loggedInUser&quot;) != null) {&#13;&#10;            User user = (User) session.getAttribute(&quot;loggedInUser&quot;);&#13;&#10;            redirect(user, req, res);&#13;&#10;            return;&#13;&#10;        }&#13;&#10;        req.getRequestDispatcher(&quot;/WEB-INF/view/login.jsp&quot;).forward(req, res);&#10;    }&#13;&#10;&#13;&#10;    @Override&#13;&#10;    protected void doPost(HttpServletRequest req, HttpServletResponse res)&#13;&#10;            throws ServletException, IOException {&#13;&#10;&#13;&#10;        String email    = ValidationUtil.sanitize(req.getParameter(&quot;email&quot;));&#13;&#10;        String password = req.getParameter(&quot;password&quot;);&#13;&#10;&#13;&#10;        // Basic input check&#13;&#10;        if (!ValidationUtil.isValidEmail(email) || !ValidationUtil.isNotEmpty(password)) {&#13;&#10;            req.setAttribute(&quot;error&quot;, &quot;Please enter a valid email and password.&quot;);&#13;&#10;            req.getRequestDispatcher(&quot;/WEB-INF/view/login.jsp&quot;).forward(req, res);&#10;            return;&#13;&#10;        }&#13;&#10;&#13;&#10;        try {&#13;&#10;            User user = userDAO.findByEmail(email);&#13;&#10;&#13;&#10;            if (user == null || !PasswordUtil.verify(password, user.getPassword())) {&#13;&#10;                req.setAttribute(&quot;error&quot;, &quot;Invalid email or password.&quot;);&#13;&#10;                req.getRequestDispatcher(&quot;/WEB-INF/view/login.jsp&quot;).forward(req, res);&#10;                return;&#13;&#10;            }&#13;&#10;&#13;&#10;            if (user.isStudent() &amp;&amp; !user.isApprovedUser()) {&#13;&#10;                req.setAttribute(&quot;error&quot;, &quot;Your account is pending admin approval.&quot;);&#13;&#10;                req.getRequestDispatcher(&quot;/WEB-INF/view/login.jsp&quot;).forward(req, res);&#10;                return;&#13;&#10;            }&#13;&#10;&#13;&#10;            // Create session&#13;&#10;            HttpSession session = req.getSession(true);&#13;&#10;            session.setAttribute(&quot;loggedInUser&quot;, user);&#13;&#10;            session.setMaxInactiveInterval(30 * 60);  // 30 minutes&#13;&#10;&#13;&#10;            // Set remember-me cookie (optional)&#13;&#10;            String rememberMe = req.getParameter(&quot;rememberMe&quot;);&#13;&#10;            if (&quot;on&quot;.equals(rememberMe)) {&#13;&#10;                Cookie cookie = new Cookie(&quot;eh_email&quot;, email);&#13;&#10;                cookie.setMaxAge(7 * 24 * 60 * 60);  // 7 days&#13;&#10;                cookie.setPath(req.getContextPath());&#13;&#10;                cookie.setHttpOnly(true);&#13;&#10;                res.addCookie(cookie);&#13;&#10;            }&#13;&#10;&#13;&#10;            redirect(user, req, res);&#13;&#10;&#13;&#10;        } catch (Exception e) {&#13;&#10;            req.setAttribute(&quot;error&quot;, &quot;A server error occurred. Please try again.&quot;);&#13;&#10;            req.getRequestDispatcher(&quot;/WEB-INF/view/login.jsp&quot;).forward(req, res);&#10;        }&#13;&#10;    }&#13;&#10;&#13;&#10;    private void redirect(User user, HttpServletRequest req, HttpServletResponse res)&#13;&#10;            throws IOException {&#13;&#10;        String ctx = req.getContextPath();&#13;&#10;        if (user.isAdmin())   res.sendRedirect(ctx + &quot;/admin/dashboard&quot;);&#13;&#10;        else                  res.sendRedirect(ctx + &quot;/student/dashboard&quot;);&#13;&#10;    }&#13;&#10;}&#13;&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/src/main/java/controller/RegisterServlet.java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/src/main/java/controller/RegisterServlet.java" />
              <option name="originalContent" value="package controller;&#10;&#10;import dao.UserDAO;&#10;import jakarta.servlet.ServletException;&#10;import jakarta.servlet.annotation.WebServlet;&#10;import jakarta.servlet.http.*;&#10;import model.User;&#10;import util.PasswordUtil;&#10;import util.ValidationUtil;&#10;&#10;import java.io.IOException;&#10;import java.sql.Date;&#10;&#10;/**&#10; * RegisterServlet - Handles student registration.&#10; * URL: /register&#10; */&#10;@WebServlet(&quot;/register&quot;)&#10;public class RegisterServlet extends HttpServlet {&#10;&#10;    private final UserDAO userDAO = new UserDAO();&#10;&#10;    @Override&#10;    protected void doGet(HttpServletRequest req, HttpServletResponse res)&#10;            throws ServletException, IOException {&#10;        req.getRequestDispatcher(&quot;/WEB-INF/views/register.jsp&quot;).forward(req, res);&#10;    }&#10;&#10;    @Override&#10;    protected void doPost(HttpServletRequest req, HttpServletResponse res)&#10;            throws ServletException, IOException {&#10;&#10;        String fullName  = ValidationUtil.sanitize(req.getParameter(&quot;fullName&quot;));&#10;        String email     = ValidationUtil.sanitize(req.getParameter(&quot;email&quot;));&#10;        String phone     = ValidationUtil.sanitize(req.getParameter(&quot;phone&quot;));&#10;        String password  = req.getParameter(&quot;password&quot;);&#10;        String confirm   = req.getParameter(&quot;confirmPassword&quot;);&#10;        String dob       = req.getParameter(&quot;dateOfBirth&quot;);&#10;        String course    = ValidationUtil.sanitize(req.getParameter(&quot;course&quot;));&#10;        String level     = ValidationUtil.sanitize(req.getParameter(&quot;level&quot;));&#10;        String yearStr   = req.getParameter(&quot;year&quot;);&#10;&#10;        // ── Validation ────────────────────────────────────────────────────&#10;        if (!ValidationUtil.isValidName(fullName)) {&#10;            setErrorAndForward(req, res, &quot;Full name must contain letters only (2–100 chars).&quot;);&#10;            return;&#10;        }&#10;        if (!ValidationUtil.isValidEmail(email)) {&#10;            setErrorAndForward(req, res, &quot;Please enter a valid email address.&quot;);&#10;            return;&#10;        }&#10;        if (!ValidationUtil.isValidPhone(phone)) {&#10;            setErrorAndForward(req, res, &quot;Phone number must be exactly 10 digits.&quot;);&#10;            return;&#10;        }&#10;        if (!ValidationUtil.isValidPassword(password)) {&#10;            setErrorAndForward(req, res,&#10;                    &quot;Password must be at least 8 characters, with one uppercase letter, &quot; +&#10;                            &quot;one number, and one special character (@$!%*?&amp;).&quot;);&#10;            return;&#10;        }&#10;        if (!password.equals(confirm)) {&#10;            setErrorAndForward(req, res, &quot;Passwords do not match.&quot;);&#10;            return;&#10;        }&#10;&#10;        try {&#10;            // ── Uniqueness checks ──────────────────────────────────────────&#10;            if (userDAO.emailExists(email)) {&#10;                setErrorAndForward(req, res, &quot;An account with this email already exists.&quot;);&#10;                return;&#10;            }&#10;            if (userDAO.phoneExists(phone)) {&#10;                setErrorAndForward(req, res, &quot;An account with this phone number already exists.&quot;);&#10;                return;&#10;            }&#10;&#10;            // ── Build User object ──────────────────────────────────────────&#10;            User user = new User();&#10;            user.setFullName(fullName);&#10;            user.setEmail(email);&#10;            user.setPhone(phone);&#10;            user.setPassword(PasswordUtil.hash(password));&#10;            user.setDateOfBirth(Date.valueOf(dob));&#10;            user.setCourse(course);&#10;            user.setLevel(level);&#10;            user.setYear(yearStr != null &amp;&amp; !yearStr.isEmpty() ? Integer.parseInt(yearStr) : 0);&#10;&#10;            // ── Persist ────────────────────────────────────────────────────&#10;            if (userDAO.registerUser(user)) {&#10;                userDAO.insertStudentDetails(user);&#10;                req.setAttribute(&quot;success&quot;,&#10;                        &quot;Registration successful! Please wait for admin approval before logging in.&quot;);&#10;                req.getRequestDispatcher(&quot;/WEB-INF/views/login.jsp&quot;).forward(req, res);&#10;            } else {&#10;                setErrorAndForward(req, res, &quot;Registration failed. Please try again.&quot;);&#10;            }&#10;&#10;        } catch (Exception e) {&#10;            setErrorAndForward(req, res, &quot;A server error occurred. Please try again.&quot;);&#10;        }&#10;    }&#10;&#10;    private void setErrorAndForward(HttpServletRequest req, HttpServletResponse res, String msg)&#10;            throws ServletException, IOException {&#10;        req.setAttribute(&quot;error&quot;, msg);&#10;        req.getRequestDispatcher(&quot;/WEB-INF/views/register.jsp&quot;).forward(req, res);&#10;    }&#10;}&#10;" />
              <option name="updatedContent" value="package controller;&#13;&#10;&#13;&#10;import dao.UserDAO;&#13;&#10;import jakarta.servlet.ServletException;&#13;&#10;import jakarta.servlet.annotation.WebServlet;&#13;&#10;import jakarta.servlet.http.*;&#13;&#10;import model.User;&#13;&#10;import util.PasswordUtil;&#13;&#10;import util.ValidationUtil;&#13;&#10;&#13;&#10;import java.io.IOException;&#13;&#10;import java.sql.Date;&#13;&#10;&#13;&#10;/**&#13;&#10; * RegisterServlet - Handles student registration.&#13;&#10; * URL: /register&#13;&#10; */&#13;&#10;@WebServlet(&quot;/register&quot;)&#13;&#10;public class RegisterServlet extends HttpServlet {&#13;&#10;&#13;&#10;    private final UserDAO userDAO = new UserDAO();&#13;&#10;&#13;&#10;    @Override&#13;&#10;    protected void doGet(HttpServletRequest req, HttpServletResponse res)&#13;&#10;            throws ServletException, IOException {&#13;&#10;        req.getRequestDispatcher(&quot;/WEB-INF/view/register.jsp&quot;).forward(req, res);&#10;    }&#13;&#10;&#13;&#10;    @Override&#13;&#10;    protected void doPost(HttpServletRequest req, HttpServletResponse res)&#13;&#10;            throws ServletException, IOException {&#13;&#10;&#13;&#10;        String fullName  = ValidationUtil.sanitize(req.getParameter(&quot;fullName&quot;));&#13;&#10;        String email     = ValidationUtil.sanitize(req.getParameter(&quot;email&quot;));&#13;&#10;        String phone     = ValidationUtil.sanitize(req.getParameter(&quot;phone&quot;));&#13;&#10;        String password  = req.getParameter(&quot;password&quot;);&#13;&#10;        String confirm   = req.getParameter(&quot;confirmPassword&quot;);&#13;&#10;        String dob       = req.getParameter(&quot;dateOfBirth&quot;);&#13;&#10;        String course    = ValidationUtil.sanitize(req.getParameter(&quot;course&quot;));&#13;&#10;        String level     = ValidationUtil.sanitize(req.getParameter(&quot;level&quot;));&#13;&#10;        String yearStr   = req.getParameter(&quot;year&quot;);&#13;&#10;&#13;&#10;        // ── Validation ────────────────────────────────────────────────────&#13;&#10;        if (!ValidationUtil.isValidName(fullName)) {&#13;&#10;            setErrorAndForward(req, res, &quot;Full name must contain letters only (2–100 chars).&quot;);&#13;&#10;            return;&#13;&#10;        }&#13;&#10;        if (!ValidationUtil.isValidEmail(email)) {&#13;&#10;            setErrorAndForward(req, res, &quot;Please enter a valid email address.&quot;);&#13;&#10;            return;&#13;&#10;        }&#13;&#10;        if (!ValidationUtil.isValidPhone(phone)) {&#13;&#10;            setErrorAndForward(req, res, &quot;Phone number must be exactly 10 digits.&quot;);&#13;&#10;            return;&#13;&#10;        }&#13;&#10;        if (!ValidationUtil.isValidPassword(password)) {&#13;&#10;            setErrorAndForward(req, res,&#13;&#10;                    &quot;Password must be at least 8 characters, with one uppercase letter, &quot; +&#13;&#10;                            &quot;one number, and one special character (@$!%*?&amp;).&quot;);&#13;&#10;            return;&#13;&#10;        }&#13;&#10;        if (!password.equals(confirm)) {&#13;&#10;            setErrorAndForward(req, res, &quot;Passwords do not match.&quot;);&#13;&#10;            return;&#13;&#10;        }&#13;&#10;&#13;&#10;        try {&#13;&#10;            // ── Uniqueness checks ──────────────────────────────────────────&#13;&#10;            if (userDAO.emailExists(email)) {&#13;&#10;                setErrorAndForward(req, res, &quot;An account with this email already exists.&quot;);&#13;&#10;                return;&#13;&#10;            }&#13;&#10;            if (userDAO.phoneExists(phone)) {&#13;&#10;                setErrorAndForward(req, res, &quot;An account with this phone number already exists.&quot;);&#13;&#10;                return;&#13;&#10;            }&#13;&#10;&#13;&#10;            // ── Build User object ──────────────────────────────────────────&#13;&#10;            User user = new User();&#13;&#10;            user.setFullName(fullName);&#13;&#10;            user.setEmail(email);&#13;&#10;            user.setPhone(phone);&#13;&#10;            user.setPassword(PasswordUtil.hash(password));&#13;&#10;            user.setDateOfBirth(Date.valueOf(dob));&#13;&#10;            user.setCourse(course);&#13;&#10;            user.setLevel(level);&#13;&#10;            user.setYear(yearStr != null &amp;&amp; !yearStr.isEmpty() ? Integer.parseInt(yearStr) : 0);&#13;&#10;&#13;&#10;            // ── Persist ────────────────────────────────────────────────────&#13;&#10;            if (userDAO.registerUser(user)) {&#13;&#10;                userDAO.insertStudentDetails(user);&#13;&#10;                req.setAttribute(&quot;success&quot;,&#13;&#10;                        &quot;Registration successful! Please wait for admin approval before logging in.&quot;);&#13;&#10;                req.getRequestDispatcher(&quot;/WEB-INF/view/login.jsp&quot;).forward(req, res);&#10;            } else {&#13;&#10;                setErrorAndForward(req, res, &quot;Registration failed. Please try again.&quot;);&#13;&#10;            }&#13;&#10;&#13;&#10;        } catch (Exception e) {&#13;&#10;            setErrorAndForward(req, res, &quot;A server error occurred. Please try again.&quot;);&#13;&#10;        }&#13;&#10;    }&#13;&#10;&#13;&#10;    private void setErrorAndForward(HttpServletRequest req, HttpServletResponse res, String msg)&#13;&#10;            throws ServletException, IOException {&#13;&#10;        req.setAttribute(&quot;error&quot;, msg);&#13;&#10;        req.getRequestDispatcher(&quot;/WEB-INF/view/register.jsp&quot;).forward(req, res);&#10;    }&#13;&#10;}&#13;&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/src/main/java/controller/student/EventBrowseServlet.java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/src/main/java/controller/student/EventBrowseServlet.java" />
              <option name="originalContent" value="package controller.student;&#10;&#10;import dao.EnrollmentDAO;&#10;import dao.EventDAO;&#10;import jakarta.servlet.ServletException;&#10;import jakarta.servlet.annotation.WebServlet;&#10;import jakarta.servlet.http.*;&#10;import model.Event;&#10;import model.User;&#10;&#10;import java.io.IOException;&#10;&#10;/**&#10; * EventBrowseServlet - Student browses and searches events.&#10; * URL: /student/events&#10; */&#10;@WebServlet(&quot;/student/events&quot;)&#10;public class EventBrowseServlet extends HttpServlet {&#10;&#10;    private final EventDAO      eventDAO      = new EventDAO();&#10;    private final EnrollmentDAO enrollmentDAO = new EnrollmentDAO();&#10;&#10;    @Override&#10;    protected void doGet(HttpServletRequest req, HttpServletResponse res)&#10;            throws ServletException, IOException {&#10;&#10;        String keyword  = req.getParameter(&quot;keyword&quot;);&#10;        String category = req.getParameter(&quot;category&quot;);&#10;&#10;        try {&#10;            req.setAttribute(&quot;events&quot;,   eventDAO.searchEvents(keyword, category));&#10;            req.setAttribute(&quot;keyword&quot;,  keyword);&#10;            req.setAttribute(&quot;category&quot;, category);&#10;        } catch (Exception e) {&#10;            req.setAttribute(&quot;error&quot;, &quot;Could not load events.&quot;);&#10;        }&#10;        req.getRequestDispatcher(&quot;/WEB-INF/views/student/events.jsp&quot;).forward(req, res);&#10;    }&#10;}" />
              <option name="updatedContent" value="package controller.student;&#10;&#10;import dao.EventDAO;&#10;import jakarta.servlet.ServletException;&#10;import jakarta.servlet.annotation.WebServlet;&#10;import jakarta.servlet.http.*;&#10;&#10;import java.io.IOException;&#10;&#10;/**&#10; * EventBrowseServlet - Student browses and searches events.&#10; * URL: /student/events&#10; */&#10;@WebServlet(&quot;/student/events&quot;)&#10;public class EventBrowseServlet extends HttpServlet {&#10;&#10;    private final EventDAO eventDAO = new EventDAO();&#10;&#10;    @Override&#10;    protected void doGet(HttpServletRequest req, HttpServletResponse res)&#10;            throws ServletException, IOException {&#10;&#10;        String keyword  = req.getParameter(&quot;keyword&quot;);&#10;        String category = req.getParameter(&quot;category&quot;);&#10;&#10;        try {&#10;            req.setAttribute(&quot;events&quot;,   eventDAO.searchEvents(keyword, category));&#10;            req.setAttribute(&quot;keyword&quot;,  keyword);&#10;            req.setAttribute(&quot;category&quot;, category);&#10;        } catch (Exception e) {&#10;            req.setAttribute(&quot;error&quot;, &quot;Could not load events.&quot;);&#10;        }&#10;        req.getRequestDispatcher(&quot;/WEB-INF/view/student/events.jsp&quot;).forward(req, res);&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/src/main/java/controller/student/ProfileServlet.java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/src/main/java/controller/student/ProfileServlet.java" />
              <option name="originalContent" value="package controller.student;&#10;&#10;import dao.UserDAO;&#10;import jakarta.servlet.ServletException;&#10;import jakarta.servlet.annotation.WebServlet;&#10;import jakarta.servlet.http.*;&#10;import model.User;&#10;import util.PasswordUtil;&#10;import util.ValidationUtil;&#10;&#10;import java.io.IOException;&#10;import java.sql.Date;&#10;&#10;/**&#10; * ProfileServlet - View and update student profile.&#10; * URL: /student/profile&#10; */&#10;@WebServlet(&quot;/student/profile&quot;)&#10;public class ProfileServlet extends HttpServlet {&#10;&#10;    private final UserDAO userDAO = new UserDAO();&#10;&#10;    @Override&#10;    protected void doGet(HttpServletRequest req, HttpServletResponse res)&#10;            throws ServletException, IOException {&#10;        req.getRequestDispatcher(&quot;/WEB-INF/views/student/profile.jsp&quot;).forward(req, res);&#10;    }&#10;&#10;    @Override&#10;    protected void doPost(HttpServletRequest req, HttpServletResponse res)&#10;            throws ServletException, IOException {&#10;&#10;        String action = req.getParameter(&quot;action&quot;);&#10;        User   user   = (User) req.getSession().getAttribute(&quot;loggedInUser&quot;);&#10;&#10;        try {&#10;            if (&quot;updateProfile&quot;.equals(action)) {&#10;                String fullName = ValidationUtil.sanitize(req.getParameter(&quot;fullName&quot;));&#10;                String phone    = ValidationUtil.sanitize(req.getParameter(&quot;phone&quot;));&#10;                String dob      = req.getParameter(&quot;dateOfBirth&quot;);&#10;&#10;                if (!ValidationUtil.isValidName(fullName)) {&#10;                    req.setAttribute(&quot;error&quot;, &quot;Name must contain letters only.&quot;);&#10;                    req.getRequestDispatcher(&quot;/WEB-INF/views/student/profile.jsp&quot;).forward(req, res);&#10;                    return;&#10;                }&#10;                if (!ValidationUtil.isValidPhone(phone)) {&#10;                    req.setAttribute(&quot;error&quot;, &quot;Phone must be 10 digits.&quot;);&#10;                    req.getRequestDispatcher(&quot;/WEB-INF/views/student/profile.jsp&quot;).forward(req, res);&#10;                    return;&#10;                }&#10;&#10;                user.setFullName(fullName);&#10;                user.setPhone(phone);&#10;                user.setDateOfBirth(Date.valueOf(dob));&#10;                userDAO.updateProfile(user);&#10;&#10;                // Update session object&#10;                req.getSession().setAttribute(&quot;loggedInUser&quot;, userDAO.getUserById(user.getUserId()));&#10;                req.setAttribute(&quot;success&quot;, &quot;Profile updated successfully.&quot;);&#10;&#10;            } else if (&quot;changePassword&quot;.equals(action)) {&#10;                String current = req.getParameter(&quot;currentPassword&quot;);&#10;                String newPass = req.getParameter(&quot;newPassword&quot;);&#10;                String confirm = req.getParameter(&quot;confirmPassword&quot;);&#10;&#10;                if (!PasswordUtil.verify(current, user.getPassword())) {&#10;                    req.setAttribute(&quot;error&quot;, &quot;Current password is incorrect.&quot;);&#10;                    req.getRequestDispatcher(&quot;/WEB-INF/views/student/profile.jsp&quot;).forward(req, res);&#10;                    return;&#10;                }&#10;                if (!ValidationUtil.isValidPassword(newPass)) {&#10;                    req.setAttribute(&quot;error&quot;,&#10;                            &quot;New password must be 8+ chars with uppercase, number, and special character.&quot;);&#10;                    req.getRequestDispatcher(&quot;/WEB-INF/views/student/profile.jsp&quot;).forward(req, res);&#10;                    return;&#10;                }&#10;                if (!newPass.equals(confirm)) {&#10;                    req.setAttribute(&quot;error&quot;, &quot;Passwords do not match.&quot;);&#10;                    req.getRequestDispatcher(&quot;/WEB-INF/views/student/profile.jsp&quot;).forward(req, res);&#10;                    return;&#10;                }&#10;                userDAO.updatePassword(user.getUserId(), PasswordUtil.hash(newPass));&#10;                req.setAttribute(&quot;success&quot;, &quot;Password changed successfully.&quot;);&#10;            }&#10;        } catch (Exception e) {&#10;            req.setAttribute(&quot;error&quot;, &quot;Update failed. Please try again.&quot;);&#10;        }&#10;&#10;        req.getRequestDispatcher(&quot;/WEB-INF/views/student/profile.jsp&quot;).forward(req, res);&#10;    }&#10;}" />
              <option name="updatedContent" value="package controller.student;&#13;&#10;&#13;&#10;import dao.UserDAO;&#13;&#10;import jakarta.servlet.ServletException;&#13;&#10;import jakarta.servlet.annotation.WebServlet;&#13;&#10;import jakarta.servlet.http.*;&#13;&#10;import model.User;&#13;&#10;import util.PasswordUtil;&#13;&#10;import util.ValidationUtil;&#13;&#10;&#13;&#10;import java.io.IOException;&#13;&#10;import java.sql.Date;&#13;&#10;&#13;&#10;/**&#13;&#10; * ProfileServlet - View and update student profile.&#13;&#10; * URL: /student/profile&#13;&#10; */&#13;&#10;@WebServlet(&quot;/student/profile&quot;)&#13;&#10;public class ProfileServlet extends HttpServlet {&#13;&#10;&#13;&#10;    private final UserDAO userDAO = new UserDAO();&#13;&#10;&#13;&#10;    @Override&#13;&#10;    protected void doGet(HttpServletRequest req, HttpServletResponse res)&#13;&#10;            throws ServletException, IOException {&#13;&#10;        req.getRequestDispatcher(&quot;/WEB-INF/view/student/profile.jsp&quot;).forward(req, res);&#10;    }&#13;&#10;&#13;&#10;    @Override&#13;&#10;    protected void doPost(HttpServletRequest req, HttpServletResponse res)&#13;&#10;            throws ServletException, IOException {&#13;&#10;&#13;&#10;        String action = req.getParameter(&quot;action&quot;);&#13;&#10;        User   user   = (User) req.getSession().getAttribute(&quot;loggedInUser&quot;);&#13;&#10;&#13;&#10;        try {&#13;&#10;            if (&quot;updateProfile&quot;.equals(action)) {&#13;&#10;                String fullName = ValidationUtil.sanitize(req.getParameter(&quot;fullName&quot;));&#13;&#10;                String phone    = ValidationUtil.sanitize(req.getParameter(&quot;phone&quot;));&#13;&#10;                String dob      = req.getParameter(&quot;dateOfBirth&quot;);&#13;&#10;&#13;&#10;                if (!ValidationUtil.isValidName(fullName)) {&#13;&#10;                    req.setAttribute(&quot;error&quot;, &quot;Name must contain letters only.&quot;);&#13;&#10;                    req.getRequestDispatcher(&quot;/WEB-INF/view/student/profile.jsp&quot;).forward(req, res);&#10;                    return;&#13;&#10;                }&#13;&#10;                if (!ValidationUtil.isValidPhone(phone)) {&#13;&#10;                    req.setAttribute(&quot;error&quot;, &quot;Phone must be 10 digits.&quot;);&#13;&#10;                    req.getRequestDispatcher(&quot;/WEB-INF/view/student/profile.jsp&quot;).forward(req, res);&#10;                    return;&#13;&#10;                }&#13;&#10;&#13;&#10;                user.setFullName(fullName);&#13;&#10;                user.setPhone(phone);&#13;&#10;                user.setDateOfBirth(Date.valueOf(dob));&#13;&#10;                userDAO.updateProfile(user);&#13;&#10;&#13;&#10;                // Update session object&#13;&#10;                req.getSession().setAttribute(&quot;loggedInUser&quot;, userDAO.getUserById(user.getUserId()));&#13;&#10;                req.setAttribute(&quot;success&quot;, &quot;Profile updated successfully.&quot;);&#13;&#10;&#13;&#10;            } else if (&quot;changePassword&quot;.equals(action)) {&#13;&#10;                String current = req.getParameter(&quot;currentPassword&quot;);&#13;&#10;                String newPass = req.getParameter(&quot;newPassword&quot;);&#13;&#10;                String confirm = req.getParameter(&quot;confirmPassword&quot;);&#13;&#10;&#13;&#10;                if (!PasswordUtil.verify(current, user.getPassword())) {&#13;&#10;                    req.setAttribute(&quot;error&quot;, &quot;Current password is incorrect.&quot;);&#13;&#10;                    req.getRequestDispatcher(&quot;/WEB-INF/view/student/profile.jsp&quot;).forward(req, res);&#10;                    return;&#13;&#10;                }&#13;&#10;                if (!ValidationUtil.isValidPassword(newPass)) {&#13;&#10;                    req.setAttribute(&quot;error&quot;,&#13;&#10;                            &quot;New password must be 8+ chars with uppercase, number, and special character.&quot;);&#13;&#10;                    req.getRequestDispatcher(&quot;/WEB-INF/view/student/profile.jsp&quot;).forward(req, res);&#10;                    return;&#13;&#10;                }&#13;&#10;                if (!newPass.equals(confirm)) {&#13;&#10;                    req.setAttribute(&quot;error&quot;, &quot;Passwords do not match.&quot;);&#13;&#10;                    req.getRequestDispatcher(&quot;/WEB-INF/view/student/profile.jsp&quot;).forward(req, res);&#10;                    return;&#13;&#10;                }&#13;&#10;                userDAO.updatePassword(user.getUserId(), PasswordUtil.hash(newPass));&#13;&#10;                req.setAttribute(&quot;success&quot;, &quot;Password changed successfully.&quot;);&#13;&#10;            }&#13;&#10;        } catch (Exception e) {&#13;&#10;            req.setAttribute(&quot;error&quot;, &quot;Update failed. Please try again.&quot;);&#13;&#10;        }&#13;&#10;&#13;&#10;        req.getRequestDispatcher(&quot;/WEB-INF/view/student/profile.jsp&quot;).forward(req, res);&#10;    }&#13;&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/src/main/java/controller/student/StudentDashboardServlet.java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/src/main/java/controller/student/StudentDashboardServlet.java" />
              <option name="originalContent" value="package controller.student;&#10;&#10;import dao.EnrollmentDAO;&#10;import dao.EventDAO;&#10;import jakarta.servlet.ServletException;&#10;import jakarta.servlet.annotation.WebServlet;&#10;import jakarta.servlet.http.*;&#10;import model.User;&#10;&#10;import java.io.IOException;&#10;&#10;/**&#10; * StudentDashboardServlet - Student home page.&#10; * URL: /student/dashboard&#10; */&#10;@WebServlet(&quot;/student/dashboard&quot;)&#10;public class StudentDashboardServlet extends HttpServlet {&#10;&#10;    private final EventDAO      eventDAO      = new EventDAO();&#10;    private final EnrollmentDAO enrollmentDAO = new EnrollmentDAO();&#10;&#10;    @Override&#10;    protected void doGet(HttpServletRequest req, HttpServletResponse res)&#10;            throws ServletException, IOException {&#10;        try {&#10;            User user = (User) req.getSession().getAttribute(&quot;loggedInUser&quot;);&#10;            req.setAttribute(&quot;upcomingEvents&quot;,  eventDAO.getAllEvents());&#10;            req.setAttribute(&quot;myEnrollments&quot;,   enrollmentDAO.getEnrollmentsByUser(user.getUserId()));&#10;            req.setAttribute(&quot;popularEvents&quot;,   eventDAO.getPopularEvents(3));&#10;        } catch (Exception e) {&#10;            req.setAttribute(&quot;error&quot;, &quot;Could not load dashboard.&quot;);&#10;        }&#10;        req.getRequestDispatcher(&quot;/WEB-INF/views/student/dashboard.jsp&quot;).forward(req, res);&#10;    }&#10;}" />
              <option name="updatedContent" value="package controller.student;&#13;&#10;&#13;&#10;import dao.EnrollmentDAO;&#13;&#10;import dao.EventDAO;&#13;&#10;import jakarta.servlet.ServletException;&#13;&#10;import jakarta.servlet.annotation.WebServlet;&#13;&#10;import jakarta.servlet.http.*;&#13;&#10;import model.User;&#13;&#10;&#13;&#10;import java.io.IOException;&#13;&#10;&#13;&#10;/**&#13;&#10; * StudentDashboardServlet - Student home page.&#13;&#10; * URL: /student/dashboard&#13;&#10; */&#13;&#10;@WebServlet(&quot;/student/dashboard&quot;)&#13;&#10;public class StudentDashboardServlet extends HttpServlet {&#13;&#10;&#13;&#10;    private final EventDAO      eventDAO      = new EventDAO();&#13;&#10;    private final EnrollmentDAO enrollmentDAO = new EnrollmentDAO();&#13;&#10;&#13;&#10;    @Override&#13;&#10;    protected void doGet(HttpServletRequest req, HttpServletResponse res)&#13;&#10;            throws ServletException, IOException {&#13;&#10;        try {&#13;&#10;            User user = (User) req.getSession().getAttribute(&quot;loggedInUser&quot;);&#13;&#10;            req.setAttribute(&quot;upcomingEvents&quot;,  eventDAO.getAllEvents());&#13;&#10;            req.setAttribute(&quot;myEnrollments&quot;,   enrollmentDAO.getEnrollmentsByUser(user.getUserId()));&#13;&#10;            req.setAttribute(&quot;popularEvents&quot;,   eventDAO.getPopularEvents(3));&#13;&#10;        } catch (Exception e) {&#13;&#10;            req.setAttribute(&quot;error&quot;, &quot;Could not load dashboard.&quot;);&#13;&#10;        }&#13;&#10;        req.getRequestDispatcher(&quot;/WEB-INF/view/student/dashboard.jsp&quot;).forward(req, res);&#10;    }&#13;&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>